# Gym Tracker

Personal workout + body-composition tracker. Single-file app, no build step, no backend.

Live: enable GitHub Pages on this repo (Settings -> Pages -> Deploy from branch -> main -> / (root)).
